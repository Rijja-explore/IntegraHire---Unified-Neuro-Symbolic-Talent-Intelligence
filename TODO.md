# TODO (run + fix + deploy)

- [ ] Fix current Streamlit crash (ImportError related to CandidateRecord in orchestrator/merger.py).
- [ ] Restart Streamlit and confirm no errors.
- [ ] Run quick checks (`python tools/run_quick_checks.py`).
- [ ] If quick checks pass, proceed to “deploy” (Docker or keep local).

